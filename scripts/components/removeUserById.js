/*Create a function that removes any id from the users list
export const removeUserById = function(idNumber, userList) {
  const userToRemove = function(user) {
    if(user.id !== idNumber) {
      return true;
    }
  }
  const newUsersList = userList.filter(userToRemove);

  return userList = newUsersList;
};
*/

//Do the same function with a on one line using arrow syntax
